---
phase: 03-ui-integration-visual-feedback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [internal/components/status.go, internal/ui/theme.go, internal/app/model.go, internal/app/cmds.go]
autonomous: true
requirements: [UI-01, UI-03]
user_setup: []

must_haves:
  truths:
    - "Status bar shows fetch progress indicator when fetch is in progress"
    - "Status bar displays last fetch time when fetch is idle"
    - "Fetch status indicator uses consistent theme colors and icons"
    - "Status updates don't disrupt user workflow or block interface"
  artifacts:
    - path: "internal/components/status.go"
      provides: "Fetch status integration in StatusPane"
      min_lines: 120
      contains: "FetchStatus, SetFetchStatus, SetLastFetchTime"
    - path: "internal/ui/theme.go" 
      provides: "Fetch status visual styles"
      exports: ["FetchingStyle", "FetchSuccessStyle", "FetchErrorStyle"]
    - path: "internal/app/model.go"
      provides: "Status pane updates in message handlers"
      contains: "SetFetchStatus, SetLastFetchTime"
  key_links:
    - from: "internal/app/model.go"
      to: "statusPane.SetFetchStatus"
      via: "message handler calls"
      pattern: "SetFetchStatus\\(.*\\)"
    - from: "internal/components/status.go"
      to: "ui.Styles fetch styles"
      via: "style rendering"
      pattern: "styles\\.Fetch.*Style"
---

<objective>
Integrate fetch status indicators into GitZen's status bar to provide always-visible, non-intrusive feedback about auto fetch operations.

Purpose: Users can see at a glance when fetch operations are running, when they completed, and when they last occurred without disrupting their workflow.
Output: Enhanced StatusPane with fetch progress indicators and timing information.
</objective>

<execution_context>
@/home/aeron/workspace/gitzen/.opencode/get-shit-done/workflows/execute-plan.md
@/home/aeron/workspace/gitzen/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-integration-visual-feedback/03-RESEARCH.md

# Existing architecture context for UI integration
@internal/components/status.go
@internal/ui/theme.go
@internal/app/model.go

# Previous phase implementations for message patterns
@.planning/phases/01-background-operations-foundation/01-01-SUMMARY.md
@.planning/phases/02-auto-fetch-implementation/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend StatusPane with fetch status support</name>
  <files>internal/components/status.go, internal/ui/theme.go</files>
  <action>
    Extend StatusPane struct to track fetch status and timing per UI-01 requirement:

    1. Add fetch status fields to StatusPane:
       - fetchStatus (FetchStatus enum: Idle, InProgress, Success, Error)
       - lastFetchTime (time.Time for "Last: 2m ago" display)

    2. Add FetchStatus constants matching research patterns:
       ```go
       type FetchStatus int
       const (
           FetchIdle FetchStatus = iota
           FetchInProgress  
           FetchSuccess
           FetchError
       )
       ```

    3. Add public methods for status management:
       - SetFetchStatus(status FetchStatus)
       - SetLastFetchTime(t time.Time) 
       - GetFetchStatus() FetchStatus

    4. Extend theme.go with fetch status styles:
       - FetchingStyle (spinner color, typically info blue)
       - FetchSuccessStyle (success green)  
       - FetchErrorStyle (error red)
       - Add to Styles struct and NewStyles() function

    5. Update refreshContent() to include fetch indicator:
       - Show spinner "🔄" for InProgress with FetchingStyle
       - Show checkmark "✅" briefly for Success with FetchSuccessStyle  
       - Show error "❌" for Error with FetchErrorStyle
       - Show last fetch time for Idle state: "Last: 2m ago"
       - Position after branch name: "repo → branch [🔄 Fetching...]"

    Follow GitZen's Vietnamese comment conventions and existing StatusPane patterns.
  </action>
  <verify>
    <automated>go test ./internal/components -run TestStatusPane && go build ./internal/components</automated>
  </verify>
  <done>StatusPane displays fetch status indicators with proper styling and timing information</done>
</task>

<task type="auto">
  <name>Task 2: Integrate fetch status updates in message handlers</name>
  <files>internal/app/model.go, internal/app/cmds.go</files>
  <action>
    Wire fetch status updates into existing message handlers per UI-03 requirement (non-intrusive updates):

    1. Add fetch status messages to cmds.go:
       ```go
       type fetchStatusUpdateMsg struct {
           Status FetchStatus
           Timestamp time.Time
       }
       
       func updateFetchStatusCmd(status FetchStatus) tea.Cmd {
           return func() tea.Msg {
               return fetchStatusUpdateMsg{Status: status, Timestamp: time.Now()}
           }
       }
       ```

    2. Update model.Update() message handlers:
       - startupFetchMsg handler: call updateFetchStatusCmd(FetchInProgress) 
       - startupFetchResultMsg handler: call updateFetchStatusCmd based on msg.Success
       - autoFetchResultMsg handler: call updateFetchStatusCmd based on msg.Success
       - backgroundTickMsg handler: call updateFetchStatusCmd(FetchInProgress) when starting fetch

    3. Add fetchStatusUpdateMsg handler in model.Update():
       - Call m.statusPane.SetFetchStatus(msg.Status)
       - Call m.statusPane.SetLastFetchTime(msg.Timestamp) for success cases
       - Preserve existing behavior (command log entries, status messages)

    4. Import time package in model.go for timestamp handling

    Maintain existing async patterns and don't modify current user workflow.
  </action>
  <verify>
    <automated>go build ./internal/app && go test ./internal/app -run TestModel</automated>
  </verify>
  <done>Fetch status updates automatically during fetch operations without disrupting user interface</done>
</task>

<task type="auto">  
  <name>Task 3: Add background fetch status lifecycle management</name>
  <files>internal/app/model.go</files>
  <action>
    Complete the fetch status lifecycle to handle all operation states per UI-01 and UI-03:

    1. Modify backgroundTickMsg handler to show fetch start indication:
       - Before calling executeAutoFetchCmd(), add updateFetchStatusCmd(FetchInProgress)
       - This provides immediate feedback when background fetch begins

    2. Enhance autoFetchResultMsg handler:
       - For Success && !Skipped: updateFetchStatusCmd(FetchSuccess) followed by timer to return to Idle
       - For Success && Skipped: keep current Idle status (no visual noise)
       - For !Success: updateFetchStatusCmd(FetchError) with timer to return to Idle
       - Use tea.Tick to auto-clear success/error states after 3 seconds

    3. Add fetch status clearing logic:
       ```go
       type fetchStatusClearMsg struct{}
       
       func clearFetchStatusCmd() tea.Cmd {
           return tea.Tick(3*time.Second, func(t time.Time) tea.Msg {
               return fetchStatusClearMsg{}
           })
       }
       ```

    4. Add fetchStatusClearMsg handler:
       - Set status back to FetchIdle
       - Only clear if current status is Success or Error (don't interrupt InProgress)

    This ensures status indicators are visible during operations but auto-clear to avoid visual clutter.
  </action>
  <verify>
    <automated>go build ./internal/app && echo "Manual verification: Status updates during fetch lifecycle"</automated>
  </verify>
  <done>Complete fetch status lifecycle with automatic clearing provides clear operation feedback without permanent visual noise</done>
</task>

</tasks>

<verification>
1. Status bar shows fetch indicators during operations (spinner while fetching, checkmark on success, X on error)
2. Last fetch time displays in status bar when idle
3. Status updates don't block or interrupt user workflow
4. Fetch status automatically clears after completion to avoid clutter
5. All styling follows GitZen theme consistency
</verification>

<success_criteria>
- UI-01 COMPLETE: Progress indicators appear during fetch operations in status bar
- UI-03 COMPLETE: Status updates are peripheral and non-intrusive
- Status bar provides continuous visibility of fetch operation state
- Visual feedback integrates seamlessly with existing GitZen interface
- No disruption to existing user workflow or TUI responsiveness
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-integration-visual-feedback/03-01-SUMMARY.md`
</output>