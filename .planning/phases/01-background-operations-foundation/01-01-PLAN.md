---
phase: 01-background-operations-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [internal/git/git.go, internal/background/manager.go, internal/app/model.go, internal/app/cmds.go]
autonomous: true
requirements: [FETCH-02, FETCH-03]

must_haves:
  truths:
    - "Background timer loop runs continuously without blocking TUI interactions"
    - "Working directory safety check prevents fetch when uncommitted changes exist"
    - "Background operations are cleanly cancelled when GitZen exits"
    - "Git commands are serialized to prevent race conditions"
  artifacts:
    - path: "internal/git/git.go"
      provides: "Working directory safety check method"
      exports: ["IsWorkingDirectoryClean"]
    - path: "internal/background/manager.go"
      provides: "Background operation orchestration with timer and serialization"
      min_lines: 80
    - path: "internal/app/model.go"
      provides: "Background timer integration in main model"
      contains: "backgroundCancel context.CancelFunc"
    - path: "internal/app/cmds.go"
      provides: "Background timer command definitions"
      contains: "backgroundTickMsg"
  key_links:
    - from: "internal/app/model.go"
      to: "internal/background/manager.go"
      via: "background timer commands"
      pattern: "backgroundTickCmd"
    - from: "internal/background/manager.go"
      to: "internal/git/git.go"
      via: "safety check before operations"
      pattern: "IsWorkingDirectoryClean"
---

<objective>
Establish async background operation infrastructure for GitZen's auto fetch feature using Bubble Tea timer patterns.

Purpose: Create the foundation for non-blocking background operations with proper safety checks and cancellation
Output: Background operation manager, working directory safety checks, timer integration in main model
</objective>

<execution_context>
@/home/aeron/workspace/gitzen/.opencode/get-shit-done/workflows/execute-plan.md
@/home/aeron/workspace/gitzen/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-background-operations-foundation/01-RESEARCH.md

# Existing GitZen architecture for pattern consistency
@internal/git/git.go
@internal/app/model.go
@internal/app/cmds.go

# Architecture and conventions
@ARCHITECTURE.md
@CONVENTIONS.md
</context>

<interfaces>
<!-- Key types and contracts from existing codebase -->

From internal/git/git.go:
```go
type Runner struct {
    RepoRoot string
}

// Existing methods pattern
func (r Runner) StatusPorcelainZ() ([]byte, error)
func (r Runner) run(timeout time.Duration, args ...string) (string, error)
```

From internal/app/model.go:
```go
type model struct {
    repoRoot string
    git      git.Runner
    // Components and UI state...
}

func (m model) Init() tea.Cmd
func (m model) Update(msg tea.Msg) (tea.Model, tea.Cmd)
```

From internal/app/cmds.go:
```go
type statusLoadedMsg struct{ Status git.Status }
type gitResultMsg struct {
    Cmd    string
    Result string
    Err    error
}

// Command pattern
func loadStatusCmd(r git.Runner) tea.Cmd
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add working directory safety check to git Runner</name>
  <files>internal/git/git.go</files>
  <action>
    Extend the existing git.Runner with working directory safety checking per FETCH-02 requirement.
    
    Add `IsWorkingDirectoryClean() (bool, error)` method to Runner struct using git status --porcelain=v1 -z pattern from research. Follow existing Runner method patterns (use r.runBytes with DefaultCmdTimeout, handle errors consistently). Empty output from git status means clean working directory.
    
    Place method in logical order near other status-related methods (after StatusPorcelainZ). Use existing error handling pattern with fmt.Errorf wrapping. Follow codebase conventions: exported method names, Vietnamese comments for public methods, consistent timeout usage.
  </action>
  <verify>
    <automated>go build -C /home/aeron/workspace/gitzen ./internal/git && echo "Git package builds successfully"</automated>
  </verify>
  <done>IsWorkingDirectoryClean method exists on Runner, returns true for clean directories, false for dirty directories, handles git command errors</done>
</task>

<task type="auto">
  <name>Task 2: Create background operations manager</name>
  <files>internal/background/manager.go</files>
  <action>
    Create new background operations manager following research Pattern 4 (command serialization) and Pattern 3 (cancellable context).
    
    Implement manager struct with mutex for git command serialization, git.Runner integration, and context-based lifecycle management. Include `New(git.Runner) *Manager`, `Start(context.Context) tea.Cmd` for timer initialization, and `ExecuteIfSafe(func() error)` for serialized operations.
    
    Use tea.Tick pattern from research with 30-second intervals. Follow GitZen conventions: package comments in Vietnamese, exported struct/methods, error handling with fmt.Errorf, timeout patterns from existing codebase. Include proper context cancellation handling for cleanup.
  </action>
  <verify>
    <automated>go build -C /home/aeron/workspace/gitzen ./internal/background && echo "Background package builds successfully"</automated>
  </verify>
  <done>Background manager exists with timer management, command serialization, context cancellation, and git.Runner integration</done>
</task>

<task type="auto">
  <name>Task 3: Integrate background operations into main app model</name>
  <files>internal/app/model.go, internal/app/cmds.go</files>
  <action>
    Integrate background operations into GitZen's main model following existing async command patterns.
    
    In model.go: Add backgroundManager field and backgroundCancel context.CancelFunc to model struct. Extend NewModel to initialize background manager. Update Init() to start background timer using tea.Batch. Add context cancellation in appropriate quit/cleanup location.
    
    In cmds.go: Add backgroundTickMsg type and backgroundTickCmd() tea.Cmd function following existing command patterns (like loadStatusCmd). Implement timer loop using tea.Tick with 30-second intervals from research. Add message handling in model's Update method for backgroundTickMsg.
    
    Follow existing patterns: message types as structs, tea.Cmd functions with Runner parameter, error handling via errMsg, consistent naming conventions. Ensure integration respects existing model architecture and doesn't disrupt current TUI operations.
  </action>
  <verify>
    <automated>go build -C /home/aeron/workspace/gitzen ./internal/app && echo "App package builds successfully"</automated>
  </verify>
  <done>Background timer integrated into main model, timer starts on app init, context cancellation on exit, background operations run every 30 seconds without blocking UI</done>
</task>

</tasks>

<verification>
Background operations infrastructure complete when:
- [ ] IsWorkingDirectoryClean method works correctly with git repositories
- [ ] Background manager compiles and follows GitZen architectural patterns
- [ ] Timer integration doesn't break existing TUI functionality
- [ ] All packages build successfully without breaking changes
</verification>

<success_criteria>
- Background timer loop runs continuously using tea.Tick pattern
- Working directory safety check prevents operations when changes exist
- Background operations serialize properly to prevent git race conditions  
- Context cancellation cleanly shuts down timers on app exit
- No disruption to existing GitZen TUI functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01-background-operations-foundation/01-01-SUMMARY.md`
</output>